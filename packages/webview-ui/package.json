{
  "name": "@openchoreo/webview-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Sidebar webview UI for OpenChoreo VSCode extension",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "preact": "^10.25.0",
    "@vscode/codicons": "^0.0.36"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
